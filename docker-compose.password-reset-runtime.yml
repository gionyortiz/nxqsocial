services:
  backend:
    # The preserved production baseline emits its Nest entrypoint under
    # dist/src. Keep the runtime command explicit for safe recreate/rollback.
    command: ["node", "dist/src/main.js"]
